import {Text, Container, SubText, WalkButton, } from './walk.styles'

const WalkPage = () => {

    const name = 'Tilly'

    return <>
        <Container>
            <WalkButton>Visit</WalkButton>
        </Container>
        
    </>
};

export default WalkPage